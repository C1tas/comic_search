!!! 5
html(lang="en")
    head
        title= pageTitle
        script(src="{{ url_for('static', filename = '1.js') }}")
    body
        h1.title PugJS - node template engine
    #container
        if youAreUsingPugJS
            p You are amazing
        else
            p Get on it!
        ul
            {% for i in temp_chapter_list %}
            li
                a(href='/reader/{{ temp_cur_comic_id }}/{{ i }}'){{ i }}
            {% endfor %}